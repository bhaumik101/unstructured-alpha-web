# seo/ — FastAPI service for Google-crawlable ticker + signal pages.
# Streamlit cannot serve server-side HTML; this fills that gap.
# Deployed separately on Render as `unstructured-alpha-seo` at seo.unstructuredalpha.com.
