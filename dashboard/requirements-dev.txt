# Dev/test-only dependencies — not needed to run the app, only to run the
# regression suite under tests/. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# Then run the whole suite with:
#   pytest
-r requirements.txt
pytest>=8.0.0
